import React from "react";

const Button = ({ styles }) => {
  return (
    <button
      type="button"
      className={`font-vazir mt-20 py-4 px-6 bg-blue-gradient font-poppins font-medium text-[18px] text-primary outline-none`}
    >
      شروع کنید{" "}
    </button>
  );
};

export default Button;
